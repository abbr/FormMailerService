{
	"emailTransport": {
		"Direct": {},
		"SMTP": {
			"host": "smtp.foo.com"
		},
		"_comment": "only the 1st entry is used"
	}
	,
	"authenticationSchemes": [
		{
			"type": "SSO",
			"_comment": "supported types: SSO, form, basic. form and basic should be the last and are mutually exclusive.",
			"userHeader": "remote_user"
		}, {
			"type": "form"
		}
	],
	"repository": {
		"type": "file",
		"_comment": "supported types: file, mongodb",
		"connection_string": "mongodb://localhost,localhost:27018,localhost:27019/formmailerservice"
	}
}